# WasmWeb.JS

1. build WasmWeb

```sh
$ cd src/WasmWeb/src
$ dotnet build # for generate dotnet exported types definitions and wasm file
```

2. build WasmWeb.JS/core
3. build WasmWeb.JS/inference
4. enjoy WasmWeb.JS/web-sample
